{
  "name": "ghostqa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adaptive web behavior testing platform for real-world user and failure scenarios.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names server,dashboard \"npm run dev --workspace @ghostqa/server\" \"npm run dev --workspace @ghostqa/dashboard\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run prisma:generate --workspace @ghostqa/server",
    "db:migrate": "npm run prisma:migrate --workspace @ghostqa/server",
    "demo:dev": "npm run dev --workspace @ghostqa/demo-target",
    "demo:baseline": "npm run build --workspace @ghostqa/shared --workspace @ghostqa/test-engine && npm run baseline --workspace @ghostqa/demo-target",
    "demo:test:smoke": "npm run build --workspace @ghostqa/shared --workspace @ghostqa/test-engine && npm run test:smoke --workspace @ghostqa/demo-target",
    "playwright:install": "npm run playwright:install --workspace @ghostqa/test-engine"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "concurrently": "^10.0.5",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0"
  },
  "engines": {
    "node": ">=20"
  }
}

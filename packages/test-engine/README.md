# GhostQA Test Engine

Reusable Playwright execution package.

The engine should eventually expose clean interfaces for:

- baseline execution
- scenario execution
- evidence collection
- result classification
- trace/screenshot artifact generation

Do not depend on GhostShop-specific routes, copy, or selectors.
